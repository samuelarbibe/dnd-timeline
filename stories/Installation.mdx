# Installation

To install, all you need to to is run:

```sh
npm install @samuelarbibe/react-gantt
```

Make sure you have the peer dependencies installed in you project. If not, run:

```sh
npm install react
```
