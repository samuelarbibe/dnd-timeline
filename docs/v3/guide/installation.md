---
metaLinks:
  alternates:
    - https://app.gitbook.com/s/80Y6kfGWIGJve16WSQR3/guide/installation
---

# Installation

The library requires a single peer-dependency: `react`

To install it, run:

```
npm install react
```

Then, you can install the library itself:

```
npm install dnd-timeline
```
